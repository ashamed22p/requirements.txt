services:
  - type: web
    name: arabic-image-translator
    env: python
    buildCommand: pip install -r render_requirements.txt
    startCommand: python main_enhanced.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0